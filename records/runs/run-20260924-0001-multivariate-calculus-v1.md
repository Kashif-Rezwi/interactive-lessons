# RUN-20260924-0001: Multivariate calculus v1 (first governed generation)

**Status:** Pilot complete  
**Parent run:** none (first generation for this source)  
**Owner:** Repository maintainer (solo Stage 1 operator)  
**Objective:** Governed first generation of the Class 4 interactive lesson (multivariate calculus for machine learning) from the source notebook, at benchmark-band depth: full 31-cell source pass, six-unit experience with six ladders, three prediction gates, two explain-in-own-words items, 8-item confidence-calibrated mastery, six live widgets (five canvases plus one numeric lab with a stated reason), honest dispositions for the source's transcription artifacts (including its mathematically wrong chain-rule formula and two opaque base64 figures).  
**Budget:** One generation; maximum two revision cycles  
**Iteration counts:** generation = 1; in-generation corrections = 16 (itemized below; all pre-evaluation repairs and refinements, no shift in design intent); revision cycles = 1 targeted post-evaluation remediation
**Classification:** production  
**Operating scope:** Stage 1 private pilot  
**Review-independence summary:** non-independent  
**Public-release eligibility:** ineligible

## Input manifest

- Source: [SRC-2026-0004](../sources/src-2026-0004-multivariate-calculus.md), SHA-256 `ab11029313a65ed7892248e9ecca7d26b5f19fe7c79f3cd27f64b98cd138fd3e` (re-verified at intake)
- Concept model: [CM-2026-0011](../concepts/cm-2026-0011-multivariate-calculus.md)
- Learning plan: [LP-2026-0012](../plans/lp-2026-0012-multivariate-calculus.md)
- Experience specification: [XS-2026-0012](../specifications/xs-2026-0012-multivariate-calculus-v1.md)
- Candidate: `CAN-2026-0013`, `multivariate-calculus-v1.html`
- Prompt card: `prm-generator-lesson-standard@0.6.0`, digest `532febec136b` (the card file is the persisted prompt content)
- Benchmark: [BMK-2026-0001](../benchmarks/bmk-2026-0001-linear-algebra-foundations-v4.md) (calibration exemplar, per ADR-0011); implementation reference: CAN-2026-0012 (high-dimensional geometry v1)
- Tooling: `scripts/verify-candidate.py` (strict), `scripts/check-repo.py`, Python 3 recomputation harness, Node.js 22 (`node --check` + core cross-checks), agent-browser 0.27.0 (CDP)

## Generation events

| Time | Candidate ID | Model/configuration | Prompt digests | Cost/latency | Warnings/errors |
| --- | --- | --- | --- | --- | --- |
| 2026-09-24 | CAN-2026-0013 | Cline (Claude Sonnet 4.6), autonomous orchestrator | prm-generator-lesson-standard@0.6.0 `532febec136b` + prm-orchestrator-autonomous@0.1.0 | single session | 1 assembly incident (estimation-based insert offsets on a growing 150 KB file displaced 12 chunk tails); recovered by deterministic chunk-file assembly; 1 runtime defect caught by rendered verification (Audit 6); 3 audit-driven refinements |
## Evaluation and defects

### Standing verification audits (P5 Audits 1–6)

- **Audit 1 (Coverage): PASS.** All 31 source cells dispositioned in the coverage matrix (EVAL-2026-0014): cells 1–2 → U0 (agenda; the PART 9/10 numbering inconsistency recorded); cells 3–6 → U1 (ML as optimization, loss families, L(w₁,w₂,b), millions–billions of parameters, surface-navigation metaphor, the geometric table kept as a labeled *preview* whose terms are defined in U2/U5; cell 5's opaque base64 figure replaced by the live `loss-eval` canvas); cells 7–10 → U2 (partials, freeze-others, f = x²+3y with (2,4) = 4,3 recomputed live, mountain analogy, ∂L/∂wᵢ sensitivity); cells 11–14 → U2 (gradient definition, steepest-ascent property, magnitude, negative gradient, gradient descent θ := θ − η∇f, and the source's 5·0.8²⁰ ≈ 0.0576 trajectory recomputed live); cells 15–16 → U3 (directional derivative, projection, maximum-along-gradient now *proven* via the cos φ bound, the [3,4] ‖∇f‖ = 5 example); cells 17–20 → U4 (nested compositions, the source's chain-rule formula corrected with a tagged note, computational graphs, backpropagation as repeated chain rule, x = 2 → dy/dx = 4 recomputed live); cells 21–23 → U5 (Jacobian and its (outputs × inputs) shape; the [[2,1],[0,3]] matrix shown as a live transform); cells 24–29 → U5 (Hessian, curvature, definiteness classification, saddles, Newton's method with an added invertibility guard; cell 29's opaque figure replaced by the live `curv-lab` cross-sections); cells 30–31 → U6 (non-convex landscapes, vanishing/exploding gradients grounded by λᵏ, adaptive optimizers with a one-line mechanism, stochastic/distributed optimization). No silent drops; every source topic is accounted for.
- **Audit 2 (Mathematical & canvas extrema): PASS.** Independent Python recomputation (47 checks, `RESULT: ALL PASS`): every prose number (worked MSE = 1; least-squares w* = 1.7, b* = −0.1, MSE* = 0.22; source partials 4, 3; 5·0.8²⁰ = 0.057646; chain product 4; ‖(3,4)‖ = 5; W2 default ∇f = (2.4, 3.2), ‖∇f‖ = 4; arrow-scale extremum 8.7658; θ* = 53.13°; D_u f(60°) = 3.971; flat angle 143.13°; 2/λ_max = 0.5; 0.9¹⁰⁰ = 2.656e−5), all 12 ladder keys, all 6 unit-check keys, all 3 numeric mastery keys, and all 11 MCQ/gate answer letters — each cross-checked against the page's stored key within its declared tolerance. Widget results are computed live (no hard-coded outcomes). Canvas extrema forcing (Audit-6 trace): W2 driven to all four corners and the origin (partials ±5/±7.2, plus the origin's "every partial is 0" degenerate note); W3 to η = 0.95 (path leaves the window after 2 steps with the DIVERGING verdict) and η = 0.05 from (3, −2); W4 to 53° and 143° (aligned and flat); W5 to x = 0 (product = 0, flat-composition note); W6 to (−2, 3) saddle, (0, 5) degenerate, (−3, −3) dome; W7 to 0.6¹⁰⁰ = 6.53e−23 (VANISHED) and 1.2¹⁰⁰ = 8.28e7 (EXPLODED); W1 to (w, b) = (−2, −4) and (4, 6) (MSE 252.6 / 179.8, no crash, canvas clipping only). Nothing rendered off-canvas and nothing threw.
- **Audit 3 (Dependency order): PASS.** Read-in-order re-pass over the assembled artifact: U0 (orientation, labels, loop, concept map with the dashed payoff edge) → U1 (loss/optimization; the loss explorer) → U2 (partials → gradient → gradient descent; the steepest-ascent claim stated as an explicit promise) → U3 (directional derivative; the promise proven with the cos φ bound; the plateau foreshadowed) → U4 (chain rule; per-layer factors named as Unit 6's subject) → U5 (Jacobian; Hessian; definiteness; saddles; Newton's method with its guard) → U6 (λᵏ; landscape challenges; adaptive optimizers; the flat-perpendicular and saddle loops closed) → synthesis (map revisited; the preview table now a summary) → mastery → review → glossary. Source ordering defects repaired and labeled: the cell-6 geometric table is presented as a *preview* with definitions deferred to U2/U5; the cell-12 steepest-ascent assertion is delivered as a promise paid off in U3; the cell-18 chain-rule formula is corrected inline with a `source (formula corrected)` tag. No use-before-explain findings; every forward reference is a promise with a named payoff unit.
- **Audit 4 (Pedagogical & depth-calibration contract): PASS.** Unit anatomy in all six units (Learn → Predict → Explore → Practice → Check → Connect); a lede and a worked number before the first widget in every unit; six 3-rung faded ladders (worked → completion → independent) with never-auto-opening hints; every unit check carries one auto-graded numeric item plus one diagnostic MCQ (and U2/U4 additionally carry the explain items); two explain-in-own-words items (U2, U4) with model answers and self-evaluation prompts; mastery sized 6 + 2 = 8 items, interleaved, with reasoning + transfer + error-identification items and 3-level confidence; zero `<textarea>`; one callout per unit (six total, mechanically verified); glossary covers 44 terms with six fields each; the concept map is a multi-branch dependency graph and is revisited in Synthesis.
- **Audit 5 (Technical & behavioral simulation): PASS.** `verify-candidate.py` strict mode (prompt card @0.6.0 detected): `PASSED (0 failures, 9 note(s))` — provenance comment, colophon, zero external requests, 0 duplicate IDs (201 unique), wiring targets resolve, 44 glossary entries, 13/13 range inputs encapsulated in `.slider-track` inside `.slider-control`, body-font floor ≥ 16px in all screen declarations, `.option-stack`/`.option-item` present, 15 `.formula` blocks with 15 `.symkey` lists, callout density ≤ 1 per unit. `node --check` on the extracted script: OK. Structural counts: 6 units, 3 gates, 12 ladder buttons, 12 hints, 6 numeric checks, 6 MCQs, 3 numeric mastery items, 5 MCQ mastery items, 2 explain items, 6 canvases with 6 resize listeners and 6 `makeView` calls. Behavioral traces (agent-browser): gate refusal with no selection ("Commit to an answer first…", widget stays locked); gate unlock on commitment (w2/w4/w6 revealed, canvases drawn, readouts populated) with correct-option confirmation for all three gates; ladder empty input ("Enter a number first."); MCQ without selection ("Pick an option first."); mastery without confidence (prompt); tolerance boundary at 12.05 (pass) and 12.06 (fail) after the epsilon fix; wrong-option feedback naming the specific misconception; confident-miss routing on both a numeric item (M4) and a radio item (M2) into the review list; reset clearing dots, review, and localStorage; recovery from hand-corrupted localStorage (`mvc1-dots`/`mvc1-review` invalid JSON → graceful fallback with 0 errors).
- **Audit 6 (Rendered-output verification per ADR-0010): PASS (live browser).** agent-browser 0.27.0 / CDP: 0 console messages and 0 page errors after load and after the full interaction suite; title `Multivariate Calculus for Machine Learning · Interactive Lessons v1`; body font 16.5px; 6 canvases; 13 range inputs; no horizontal overflow at any breakpoint (`scrollWidth == innerWidth` measured at 1280, 1024, 640, and 320 px — no clipping or side-scroll at 320 px); canvas client geometry 697×813 (cv1, aspect 1.17) and 697×533 (cv2–cv6, aspect 0.76) at 1280 px; sliders 339 px (1024) / 281 px (640) / 262 px (320) wide, so controls stay usable on the narrowest viewport; gated widgets hidden at load and drawn on unlock; live readouts correct at defaults (MSE 3.35; descent factors 0.7/0.4; chain values g = 4, y = 5, dy/dx = 4; λᵏ = 1.15e−2 in the usable band); the hand-tuning payoff lands exactly (w = 1.7, b = −0.1 → MSE 0.22, the live-computed least-squares optimum); reduced-motion emulation → `scroll-behavior: auto`; print PDF exported; screenshots captured at 1024/640/320 px (`/tmp/mvc1-1024.png`, `/tmp/mvc1-640.png`, `/tmp/mvc1-320.png`; the evaluator has no image input, so the measured geometry is the evidence of record).
### Adversarial re-examination (mandatory gate per ADR-0009)

- Re-examination method(s): (1) read-in-order dependency re-pass from a fresh perspective, independent of the Audit 3 notes; (2) handler-level behavioral simulation (gate refusal/unlock across branches, grading boundaries, confident-miss routing on radio *and* numeric items, reset from corrupted storage); (3) canvas-extrema forcing at every manipulable's extremes and degenerate values; (4) honesty/provenance scan over every forward promise, formula, and ML claim.
- Elements covered: all 3 gates, all 12 ladder rungs, all 6 checks, all 8 mastery items, both explain items, all 6 widgets at boundary values, storage reset, corrupted-storage recovery, and the full text of all six units for promise and honesty issues.
- Findings & severity: **three findings, all Minor, all repaired before evaluation** (revision cycles stayed 0 because no post-evaluation change was required): (a) exact-boundary numeric entries landed on the fail side by floating-point epsilon — fixed by adding 10⁻⁹ slack to the three tolerance comparisons; (b) five jargon/honesty refinements from the read-in-order and honesty scans — a *least squares* definition plus glossary entry replacing an undefined usage inside the live readout, an undefined "trust-region methods" reference reworded into plain language, one-line glosses for cross-entropy/reconstruction losses and for normalizing flows / diffusion models / sensitivity analysis, and a "gradient clipping" reference reworded into a plain description inside a glossary entry; (c) the W1 intercept slider's 0.5 step made the exact optimum (b* = −0.1) unreachable by hand — fixed to step 0.1 and verified live (MSE 0.22 at the optimum). No Critical or Major findings; no unsupported claims; every forward promise has a realized payoff.

### Re-verification pass (WF-008)

- Sampled checks re-executed after the final edit: `verify-candidate.py` strict (PASSED, 0 failures); `node --check` (SYNTAX OK); the 47-check Python recomputation (ALL PASS); and a rendered re-load in a clean session (0 errors, all readouts correct, optimum reachable, all 43 glossary links resolving).
- Headline claims reproduced: yes — no discrepancies at the final hash `5363d7d5…`.

## Reflection and root-cause hypothesis

The generation itself went as designed (single session, no design-level rework, no revision cycles), but the session surfaced a **tooling-hazard defect class**: assembling a ~150 KB single-file artifact by issuing `insert_line` appends with *estimated* offsets into a growing file. Whenever an estimate fell short of the true end of file, the insert landed *inside* the previous chunk, displacing that chunk's tail below the new content; twelve such events cascaded (six JS functions, four function tails, the glossary's four-entry boundary, and two CSS rules), plus one undefined-variable typo inside a restored function, and one `<script>` region that looked contiguous while missing its predecessors' tails. Static analysis caught the syntax imbalance (`node --check` reported unclosed braces) but the runtime `ReferenceError` was only visible in a real browser — direct evidence for ADR-0010's rendered-verification requirement. Root cause: estimation-based editing rather than deterministic assembly. Remedy adopted here and recommended as a reusable rule: build long artifacts as independent chunk files and concatenate them with one shell command; when appending is unavoidable, probe the true EOF so the tool reports the exact boundary instead of computing it. Promoted to memory as MEM-2026-0007.
### Revision history and regression checks

Post-evaluation remediation cycle 1 addressed the specialist review findings: corrected the gradient-descent path wording, made unit/mastery review entries clear on a later correct answer with per-assessment keys, added `aria-describedby` links from all six canvases to their live readouts, guarded non-array review storage, preserved legacy `chkN`/`mastery` review entries through migration, and rebuilt the concept map from the authoritative 16-edge dependency list with bounded labels and a narrow-screen internal scroll surface. Regression checks: strict candidate verification passed with 0 failures; `node --check` passed; a wrong Unit 1 numeric answer queued and a correct retry removed the item; a confident M2 mastery miss queued and a correct retry removed the item; legacy review entries survived migration and the related Unit 1 correction removed only its legacy item; all six canvas description targets resolved; every map edge was present exactly once and every label fit its node; the 320px map remained internally scrollable without page overflow; corrupted review/dot storage loaded with 0 page errors; and the browser trace reported 0 page errors and 0 console errors/warnings. Remediated candidate SHA-256: `2edbbbbe6203d43093e2d2bd7c0b25185a30ee6795bca3435684c423cca072b5`.

In-generation corrections, each with their regression check:

| # | Correction (pre-evaluation) | Regression check |
| --- | --- | --- |
| 1 | Rebuilt the artifact from chunk files after a shell-heredoc mangle | Structure counts: 11 sections, 11 closers, all unit markers in order |
| 2–7 | Restored displaced JS: `dots`/`review`/`addReview`/`renderReview`/`setDot`/`markCheck`; `gAnswer`/`bindGates`; `drawW1` readout tail; `drawW3` body and readout; `drawW5` text and readout; `drawW7` body | `node --check` plus browser behavioral traces for every restored path |
| 8–9 | Restored `bindLadders` and `bindChecks` closing braces (located by a brace-depth scan) | Ladder, check, hint, and explain traces all pass |
| 10–11 | Re-spliced the glossary entries in correct order (43 at that point; 44 in the final artifact after correction 14) and restored two displaced CSS rules (`.callout.good`, `.callout.info`) | 44 `gitem` entries present; callout styling verified in the browser |
| 12 | `dyg` → `dydg` typo in `drawW5` (runtime ReferenceError caught by Audit 6) | 0 page errors; r5 readout correct at x = 2 and x = 0 |
| 13 | Numeric tolerance ε (10⁻⁹) in ladder/check/mastery grading (adversarial boundary finding) | 12.05 passes, 12.06 fails |
| 14 | Five jargon/honesty refinements (least-squares glossary entry and link; trust-region reworded; loss-family glosses; application glosses; gradient-clamping reword) | 43 `data-term` links all resolve; zero deferred-jargon patterns |
| 15 | W1 intercept slider step 0.5 → 0.1 | Optimum reachable: (1.7, −0.1) → MSE 0.22 |
| 16 | XS re-pinned to the shipped artifact (W1/W2 viewports and slider bounds, term registry 44) | Spec↔artifact element-by-element conformance sweep |

## Decision and approvers

**Final candidate identity at closure:** `CAN-2026-0013`, `multivariate-calculus-v1.html`, SHA-256 `2edbbbbe6203d43093e2d2bd7c0b25185a30ee6795bca3435684c423cca072b5`, 159,387 bytes
**Disposition:** private-pilot-complete  
**Decision scope:** private pilot  
**Approvers and limitations:** repository maintainer (solo Stage 1 operator); non-independent review; ineligible for public release

## Memory disposition

- **Promoted:** MEM-2026-0007 — deterministic chunk-file assembly for long artifacts, plus the standing lesson that rendered verification (ADR-0010) catches runtime defects that static syntax checks pass.
- **Rejected observations:** (a) "the source's wrong chain-rule formula should be treated as a lesson defect" — rejected: it is a *source* transcription artifact, dispositioned and corrected in the artifact with a tag, which is the governed handling; (b) "gradient descent should be demonstrated on Unit 1's actual MSE surface" — rejected for scope: the U2 descent lab runs on a visualizable quadratic with a closed-form stability threshold, and the U1 readout states the MSE optimum so the payoff is explicit; recorded as a possible future enhancement, not a defect.
- **Carried forward:** the assembly-hazard rule to the pattern catalog and to any future long-artifact run.

## Lineage audit

Source `ab110293…` → SRC-2026-0004 → CM-2026-0011 → LP-2026-0012 → XS-2026-0012 → CAN-2026-0013 (`5363d7d5…`) → EVAL-2026-0014. All links resolve in-repo; the prompt snapshot is in Appendix A; no session-only knowledge is required to reproduce the run.

## Appendix A: Prompt snapshot

User trigger: "hey there, lets build another high quality interactive lession for 'content/aiml-4/module-02-math-statistics-for-ml/sources/Multivariate_Calculus_for_Machine_Learning.ipynb'"

Workflow directive: governed P0–P6 lesson generation for the Class 4 source — SRC (intake + hash `ab110293…`), CM (48 anchored claims, 35 concepts, 16 misconceptions, artifact dispositions for the corrected chain-rule formula, the PART 9/10 numbering gap, and the two opaque figures), LP (6 units + orientation, depth pass, 3 prediction gates, 2 explain items, 8-item mastery, benchmark citation), XS (7 widgets: W1–W6 canvases with declared viewports, W7 numeric with a stated reason; 15-equation formula manifest; 44-term registry; concept-map edges), single-file generation per prompt card @0.6.0, strict verification, six audits, adversarial gate, live rendered verification (agent-browser), evaluation authoring, module README update, repository checker, and compounding updates. Generator prompt card `prm-generator-lesson-standard@0.6.0`, SHA-256 `532febec136b15b4988963ad6c5ffb1477163f45013a81fd47474ab6b24c0506` (the card file is the persisted prompt content); orchestrator skill `prm-orchestrator-autonomous@0.1.0`.
